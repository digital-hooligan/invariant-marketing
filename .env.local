# invariant-marketing (S1) — Canonical Environment Template (v1)
# Copy to .env.local for local development (never commit .env.local).
#
# This repo is Public Web. It MUST NOT reference IP Vault-only env variables.
# Forbidden internal variables are hard-blocked by CI (validator prints names only).

# ---------- Universal ----------
APP_ID=invariant-marketing
ENVIRONMENT=local

# Required in preview/prod; optional in local
APP_VERSION=0.0.0-local

# Required in preview/prod (CI/build should set). Example: 2026-02-20T16:33:00Z
BUILD_TIMESTAMP=

# debug|info|warn|error
LOG_LEVEL=debug

# ---------- Optional governance controls ----------
# true|false (used by CI; safe default for local)
CI=false
# block|warn
SECRETS_SCAN_MODE=block
# true|false
ENV_CONTRACT_STRICT=true

# ---------- S1 — Public Web ----------
# Required in preview/prod
PUBLIC_SITE_URL=

# Optional (public)
NEXT_PUBLIC_ANALYTICS_ID=disabled
NEXT_PUBLIC_FEATURE_FLAGS={}
