name: build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    # Non-secret, schema-valid build env (see env.contract.json). Replaces the
    # values that used to come from the committed .env.local for `next build`.
    env:
      ENVIRONMENT: preview
      PUBLIC_SITE_URL: https://digitalhooligan.io
      APP_ID: invariant-marketing
      APP_VERSION: 0.1.0
      BUILD_TIMESTAMP: "1970-01-01T00:00:00.000Z"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Enable pnpm (corepack)
        shell: bash
        run: |
          corepack enable
          PM="$(node -p "require('./package.json').packageManager")"
          echo "packageManager=$PM"
          corepack prepare "$PM" --activate
          pnpm --version

      - name: Resolve pnpm store path
        id: pnpm-store
        run: |
          echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Build
        run: pnpm run build
